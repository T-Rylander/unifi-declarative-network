# config/firewall/wan-in.yaml — Production WAN IN Rules (default deny)
rules:
	- name: Allow established/related
		action: accept
		state: [established, related]

	- name: Drop invalid packets
		action: drop
		state: [invalid]

	- name: Default deny inbound
		action: drop
		logging: true
